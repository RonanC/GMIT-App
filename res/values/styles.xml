<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    	<!-- This style will inherit from the actionBarStyle -->
    	<item name="android:actionBarStyle">@style/MyActionBar</item>s
    </style>
    
    <style name="MyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <!-- ActionBar: Red Background, blue text , and calling white title text -->
        <item name="android:background">#862633</item>
        <item name="android:titleTextStyle">@style/MyActionBarAppearance</item>
    </style>
    
    <style name="MyActionBarAppearance" parent="android:TextAppearance.Holo.Widget.ActionBar.Title">
      	<!-- White Text in actionBar -->
        <item name="android:textColor">#ffffff</item>
    </style>
    
<!-- 	<style name="ListFont" parent="@android:style/Widget.ListView">
	    <item name="android:textColor">#ffffff</item>
	    <item name="android:typeface">sans</item>
	</style> -->
    
</resources>
